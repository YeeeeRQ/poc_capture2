[package]
name = "capture-core"
version = "0.1.0"
edition = "2021"

[dependencies]
windows-capture = "1.4"
image = "0.25"
anyhow = "1"
chrono = "0.4"
log = "0.4"
parking_lot = "0.12"
crossbeam = "0.8"
ctrlc = { version = "3", optional = true }

[features]
default = ["record"]
record = ["ctrlc"]
