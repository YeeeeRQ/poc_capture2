[package]
name = "capture-cli"
version = "1.0.0"
edition = "2021"

[dependencies]
capture-core = { path = "../capture-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
log = "0.4"
env_logger = "0.11"
